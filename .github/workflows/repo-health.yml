permissions:
  contents: read
  pull-requests: write

name: Repo Health

on:
  pull_request:
    branches: [dev, master, main]
  workflow_dispatch:

jobs:
  repo_health:
    uses: OpenVoiceOS/gh-automations/.github/workflows/repo-health.yml@02b4bf45467b832c0e03c2e15ed02daa584b8386 # dev
    with:
      version_file: 'hivemind_websocket_protocol/version.py'

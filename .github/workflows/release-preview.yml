permissions:
  contents: read
  pull-requests: write

name: Release Preview

on:
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  release_preview:
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository }}
    uses: OpenVoiceOS/gh-automations/.github/workflows/release-preview.yml@02b4bf45467b832c0e03c2e15ed02daa584b8386 # dev
    with:
      package_name: 'hivemind_websocket_protocol'
      version_file: 'hivemind_websocket_protocol/version.py'
